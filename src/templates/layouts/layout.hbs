<!doctype html>
<html lang="{{lang}}">
<head>
	<meta charset="utf-8">
	<title>{{title}}</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	{{> head-includes}}
</head>
<body>
	{{> header}}

	<main class="Main">
		{{> @partial-block}}
	</main>

	{{> footer}}

	{{> footer-includes}}
</body>
</html>
